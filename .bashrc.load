# include what you want

export PATH=$PATH:$HOME/bin

# add this to the .bashrc: . $HOME/.bashrc.load

####
# put any bash customizations in here

# Usually Debian flavors of GNU/Linux (ie. Ubuntu) have lots of custom
# shell config that I haven't incorporated into the main bashrc yet
if [ -f /etc/debian_version ]; then
    . $HOME/.backup/.bashrc
fi
